#include <stdio.h>
#include <stdlib.h>
#include <time.h>

int main() {
    srand(time(NULL));
    
    /*int kostky, steny;
    printf("Zadej pocet kostek: ");
    scanf("%d", &kostky);
    printf("Zadej pocet sten kostky: ");
    scanf("%d", &steny);
    
    int res = 0;
    int num;
    for (int i = 0; i < kostky; i++) {
        num = (rand() % steny) + 1;
        printf("%d. Hod kostkou: %d\n", i + 1, num);
        res += num;
    }
    
    printf("Soucet vsech cisel kostky: %d", res);*/
    
    int pocet_stran = 8;
    char strany[][14] = { "ANO", "SPOLU", "SPD", "STAN", "Pirati", "Motoriste", "Stacilo", "Jine" };
    float preference[] = { 29.3, 20.5, 13.4, 11.1, 10, 6, 5.5, 4.2};
    int volici = (rand() % 100000) + 1;
    
	printf("Celkem hlasu: %d\n", volici);
	for (int i = 0; i < pocet_stran; i++) {
		printf("%d. Strana: \nNazev: %s \nHlasy: %.0f \nProcent hlasicich za tuto stranu: %.1f\n", i + 1, strany[i], (volici * preference[i]) / 100, preference[i]);
	}
    
    return 0;
}
